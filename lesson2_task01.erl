-module(lesson2_task01).
-export([last/1]).

last([X]) -> X;  % Якщо список має один елемент, повертаємо його.
last([_|T]) -> last(T).  % Якщо список має більше елементів, рекурсивно шукаємо останній.
